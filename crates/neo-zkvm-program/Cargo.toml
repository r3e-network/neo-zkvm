[package]
name = "neo-zkvm-program"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = "4.0"
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
bincode = "1.3"

[build-dependencies]
sp1-build = "4.0"
